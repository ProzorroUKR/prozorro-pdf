apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prozorro-pdf.fullname" . }}-test-connection"
  labels:
    {{- include "prozorro-pdf.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prozorro-pdf.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
