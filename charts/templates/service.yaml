apiVersion: v1
kind: Service
metadata:
  name: {{ include "prozorro-pdf.fullname" . }}
  labels:
    {{- include "prozorro-pdf.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prozorro-pdf.selectorLabels" . | nindent 4 }}
